name: my-check
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run Tests
      - run: npm test
        with:
          codeclimate_api-key: ${{ secrets.CODECLIMATE_API_KEY }}